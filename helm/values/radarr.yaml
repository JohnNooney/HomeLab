controllers:
  main:
    containers:
      main:
        image:
          repository: linuxserver/radarr
          tag: latest
        env:
          TZ: "America/New_York"
          PUID: "1000"
          PGID: "1000"

service:
  main:
    ports:
      http:
        port: 7878

ingress:
  main:
    enabled: true
    hosts:
      - host: radarr.nooney.dev
        paths:
          - path: /
            pathType: Prefix

persistence:
  config:
    enabled: true
    size: 10Gi
    accessMode: ReadWriteOnce
  media:
    enabled: true
    type: hostPath
    hostPath: /mnt/media
  downloads:
    enabled: true
    type: hostPath
    hostPath: /mnt/downloads

resources:
  requests:
    cpu: 100m
    memory: 256Mi
  limits:
    cpu: 1000m
    memory: 1Gi
